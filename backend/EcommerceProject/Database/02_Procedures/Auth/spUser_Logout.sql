USE [EcommerceDB]
GO

SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE OR ALTER PROCEDURE spUser_Logout
    @UserId INT
AS
BEGIN
    SET NOCOUNT ON;

    UPDATE UserRefreshTokens
    SET IsRevoked = 1
    WHERE UserId = @UserId;

END;
